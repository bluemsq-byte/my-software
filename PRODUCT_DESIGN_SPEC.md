# 音频播放器产品设计规范

版本：1.0

适用范围：Android 本地音乐、群晖 NAS、网络音乐、播放列表、定时、最近播放、播放设备和设置

## 一、产品判断

### 1.1 产品定位

这是一款面向家庭私有音乐库的本地与 NAS 音乐播放器，不是内容推荐平台。

它的核心价值是：

- 把我拥有的音乐播放出来。
- 把手机、NAS、音箱和电视连接起来。
- 用稳定、直观的方式管理自己的音乐库。
- 不强迫用户注册平台账号，不依赖云端曲库。

### 1.2 产品气质

关键词：

- 可靠
- 安静
- 专业
- 私有
- 家庭化
- 低打扰

设计气质应当是“家庭音乐控制台”，而不是社交娱乐产品。

视觉上参考 DS Audio 的层级效率、Apple Music 的留白和网易云音乐的播放氛围，但不复制任何品牌的独占视觉资产。

### 1.3 设计原则

1. **内容优先**：封面、歌曲名、文件夹和播放状态优先于装饰。
2. **路径清晰**：本地音乐、NAS 文件夹、播放列表和队列要能快速区分。
3. **一屏可判断**：用户打开页面后能快速知道“音乐在哪里、正在播什么、下一步做什么”。
4. **毛玻璃有用途**：半透明只用于建立层级，不用于堆叠装饰。
5. **危险操作可撤回或二次确认**：删除连接、清空队列、删除播放列表必须确认。
6. **弱网和离线可理解**：NAS 断开、SMB 不可达、Cast 不可用时，给出明确原因和下一步。
7. **不依赖颜色表达状态**：同时使用图标、文字和位置表达连接、播放和错误状态。

## 二、目标用户

### 用户 A：家庭 NAS 音乐用户

- 年龄：28～50 岁。
- 设备：安卓手机、群晖 NAS、蓝牙音箱或电视。
- 特点：有大量自有音乐，习惯按文件夹管理。
- 目标：快速连接到 NAS，浏览目录并播放。
- 主要阻力：输入服务器地址、SMB 权限、路径层级复杂。

### 用户 B：家庭共享音乐使用者

- 不关心 NAS 技术细节。
- 只知道“家里有音乐服务器”。
- 目标：打开应用后直接找到最近播放或家庭共享音乐。

### 用户 C：多设备播放用户

- 使用蓝牙耳机、客厅音响、电视或 Chromecast。
- 目标：在播放页快速切换输出设备。
- 主要阻力：设备发现失败或切换后音频没有跟上。

### 用户 D：定时播放用户

- 早晨播放音乐、睡前定时停止、固定时间播放 NAS 歌单。
- 目标：设置一次后长期可靠运行。
- 主要阻力：后台限制、精确闹钟权限和 NAS 断线。

## 三、核心使用场景

### 场景 1：播放手机中的音乐

用户打开首页，在本地音乐中找到歌曲或文件夹，点击后开始播放。

优先级：P0

### 场景 2：连接群晖 NAS 并播放

用户添加 SMB 或 WebDAV 连接，浏览共享文件夹，点击歌曲开始播放。

优先级：P0

### 场景 3：把音乐发送到音箱或电视

用户在播放页选择蓝牙、系统媒体路由或 Cast 设备。

优先级：P0

### 场景 4：定时播放和停止

用户设置具体时间、重复周期和音乐来源。

优先级：P0

### 场景 5：管理播放列表

用户创建混合本地和 NAS 音乐的播放列表，点击单曲立即播放或选择多首播放。

优先级：P1

### 场景 6：继续最近播放

用户从底部导航进入最近播放，快速回到之前的音乐。

优先级：P1

## 四、信息架构

### 底部导航

1. 首页
2. 最近
3. 播放列表
4. 定时
5. 设置

迷你播放器常驻在底部导航上方。点击后进入完整播放页。

### 首页

- 搜索入口
- 本地音乐
- 网络音乐
- 文件夹模式
- 歌曲模式
- 最近播放

### 网络音乐

- 连接文件夹入口
- SMB 共享文件夹选择
- WebDAV 根目录
- 文件夹浏览
- 当前文件夹搜索

### 播放页

- 当前输出设备
- 当前封面
- 歌曲信息
- 进度条
- 播放控制
- 当前队列

### 播放列表

- 播放全部
- 添加本地
- 添加网络
- 选择模式
- 多选播放
- 排序和移除

### 定时

- 定时开始播放
- 定时停止播放
- 重复星期
- 文件选择
- 文件夹选择

### 设置

- 自动深色模式
- 浅色或深色手动选择
- 六种颜色主题
- 缓存清理
- 权限状态
- 后台播放
- 版本信息

## 五、视觉规范

### 颜色

基础色：

- 页面背景：跟随主题的中性底色。
- 主色：青绿色、蓝色、绿色等主题色。
- 主文字：高对比中性色。
- 次文字：主文字 65%～75% 明度。
- 分割线：低对比中性色。

语义色：

- 成功：绿色。
- 警告：琥珀色。
- 错误：红色。
- 禁用：中性灰，透明度 38%。
- 当前播放：主题色高亮。

### 毛玻璃

毛玻璃卡片用于：

- 歌曲行
- 文件夹行
- 连接卡片
- 播放列表卡片
- 定时任务卡片
- 播放队列歌曲行

规范：

- 背景透明度：65%～80%。
- 边框：1dp，白色或主题色 15%～20%。
- 圆角：8dp。
- 阴影：低强度，避免卡片漂浮感。
- 卡片内不能再嵌套卡片。
- 同一屏幕最多使用两级半透明层级：页面背景、卡片内容。

### 字体

使用系统无衬线字体。

- 页面大标题：28sp，600。
- 区块标题：20sp，600。
- 歌曲标题：16sp，500。
- 歌曲副标题：14sp，400。
- 辅助信息：12sp，400。
- 时间：13sp，400。

规则：

- 标题最多两行。
- 歌曲名单行省略。
- 长中文标题不得挤压右侧三点按钮。
- 文字不覆盖进度条、封面或控制按钮。

### 间距

基础间距单位：

- 4dp
- 8dp
- 12dp
- 16dp
- 24dp
- 32dp

页面左右安全边距默认 16dp，播放页封面区域可以使用 24dp。

### 图标

- 统一使用 Material Symbols。
- 默认尺寸 24dp。
- 主要按钮触摸区域不小于 48dp。
- 三点菜单统一使用 `MoreVert`。
- 文件夹、歌曲、蓝牙、Cast、播放模式必须使用不同图标。

## 六、组件规范

### 歌曲行

结构：

1. 左侧歌曲图标或封面。
2. 歌曲名称。
3. 歌手与专辑。
4. 时长。
5. 右侧三点菜单。

交互：

- 点击歌曲行：立即播放。
- 点击三点：打开更多操作。
- 长按：进入多选或拖动排序，仅在支持该行为的页面启用。

三点菜单通用操作：

- 立即播放
- 下一首播放
- 加入播放队列
- 加入播放列表

播放列表额外操作：

- 上移
- 下移
- 从列表移除

播放队列额外操作：

- 上移
- 下移
- 从队列移除

### 文件夹行

结构：

1. 文件夹图标。
2. 文件夹名称。
3. 歌曲数量。
4. 右侧进入箭头。

点击进入下一级目录。文件夹不显示三点菜单。

### 连接卡片

显示：

- 连接名称
- SMB 或 WebDAV
- 当前状态
- 进入箭头

编辑和删除放在更多菜单中。添加和编辑连接时才显示测试按钮。

### 迷你播放器

显示：

- 封面
- 歌曲名称
- 歌手
- 播放或暂停
- 下一首

点击非按钮区域进入完整播放页。

### 播放控制

- 上一首：24dp 图标按钮。
- 播放或暂停：最大主按钮，64～72dp。
- 下一首：24dp 图标按钮。
- 播放模式：单图标循环切换。
- 设备：显示当前输出设备。

## 七、页面设计

### 首页

从上到下：

1. 页面标题。
2. 搜索框。
3. 本地音乐和网络音乐分段控件。
4. 文件夹或歌曲列表。
5. 毛玻璃歌曲行和文件夹行。

### 网络文件夹页

参考 DS Audio 的文件夹页：

- 顶部返回文件夹。
- 右上提供选择、排序和播放全部。
- 文件夹头部显示封面、文件夹名称和歌曲数量。
- 歌曲列表使用毛玻璃行。
- 每首歌右侧三个点。

### 播放页

从上到下：

1. 收起按钮。
2. 当前输出设备。
3. 方形专辑封面。
4. 歌曲名称、歌手、专辑。
5. 进度条。
6. 播放控制。
7. 当前队列。

### 播放列表页

- 播放全部
- 添加本地
- 添加网络
- 选择
- 多选播放
- 三点菜单

### 定时页

- 启用状态
- 执行时间
- 重复星期
- 音乐来源
- 编辑和删除
- 开关

### 设置页

- 外观
- 主题色
- 深色模式
- 缓存
- 权限
- 后台播放
- 版本

## 八、交互与动效

- 页面进入：淡入加位移，150～300ms。
- 文件夹进入：横向滑入。
- 播放页展开：从迷你播放器向上过渡。
- 卡片点击：缩放至 98%，松开恢复。
- 三点菜单：淡入并轻微上移。
- 队列排序：长按拖动，其他歌曲平滑让位。
- 网络加载：显示轻量进度，不清空旧内容。
- 系统关闭动画时，避免持续动画。

## 九、状态设计

必须覆盖：

- 加载中
- 空数据
- 无搜索结果
- 权限未开启
- 网络不可达
- NAS 认证失败
- 文件夹不存在
- 文件格式不支持
- 投屏设备不可用
- 播放服务连接失败

错误状态必须包含原因和一个明确操作，不只显示“失败”。

## 十、文案规范

使用短句，直接说明结果。

推荐：

- 添加网络音乐
- 正在扫描本地音乐
- 未找到匹配音乐
- 连接成功
- 账号或密码错误
- NAS 暂时无法连接
- 已加入播放队列

避免：

- 技术术语堆砌
- “错误码 0x……”
- 只显示“失败”
- 过度营销或夸张文案

## 十一、无障碍

- 所有图标按钮必须有中文内容描述。
- 触摸区域不小于 48dp。
- 文字支持系统字体缩放。
- 深浅模式满足对比度要求。
- 不能只用颜色区分播放状态。
- 三点菜单必须支持 TalkBack。
- 系统减少动画时必须保留完整功能。

## 十二、响应式

第一优先：

- 360×800 Android 手机
- 412×915 Android 手机

次要支持：

- 平板纵向
- 大屏手机横屏

要求：

- 播放页在窄屏可以滚动。
- 队列不会遮挡底部控制。
- 三点菜单不会溢出屏幕。
- 长歌曲名不压住时长和菜单。

## 十三、设计验收清单

- 首页和文件夹页层级清楚。
- 网络连接使用文件夹入口，不显示测试按钮。
- 添加和编辑连接可以测试。
- 歌曲行右侧有统一三点菜单。
- 点击歌曲立即播放。
- 播放列表支持播放全部和多选播放。
- 播放页显示当前设备和完整队列。
- 毛玻璃卡片不影响文字可读性。
- 深色模式和六种主题均正常。
- 空状态、错误状态和权限状态齐全。
- 轮播、旋转和过渡遵循减少动画设置。
- 所有核心操作触摸区域不小于 48dp。

## 十四、暂不纳入

- 歌词页
- 评论和社交
- 在线音乐平台登录
- 网易云、Apple Music、DS Audio 品牌素材复制
- 云端账号体系
- 跨设备账户同步
