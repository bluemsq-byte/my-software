# 测试结果

测试日期：2026-09-16
版本：1.8.2

## 通过项

- Debug 单元测试：33 项通过。
- Release 单元测试：33 项通过。
- Android 仪器测试：20 项通过。
- Android 模拟器：Android 15，API 35，x86_64。
- Android Lint：0 个错误，通过。
- Debug APK：构建成功。
- 签名 Release APK：构建成功。
- Release APK 签名校验：通过，APK Signature Scheme v2。
- Release APK：模拟器冷启动成功，无崩溃。
- 手机端主线程网络问题：已修复并加入回归测试。
- 实际安装版本确认：versionCode 16，versionName 1.8.2。

## v1.8.2 播放列表和网络缓存

- `PlaylistRepository` 使用连接 ID 与远程路径做稳定去重。
- 播放列表“添加网络”先选择 NAS 连接，再进入文件夹多选。
- 网络多选添加支持“下载到本地”开关，下载文件保存在应用私有目录。
- 下载后的歌曲保留原 NAS 身份，避免再次添加时出现重复。
- Media3 播放网络音乐时使用 200 MB 网络缓存。
- 设置页增加“网络歌曲默认下载”选项。
- 缓存清理同时处理网络播放缓存。

## v1.8.1 首页和定时简化

- 首页使用方案 1：继续播放卡片、本地音乐和网络音乐双入口、最近播放区域。
- 底部导航取消“最近”，精简为首页、播放列表、定时和设置。
- 最近播放功能保留在首页，并提供“查看全部”入口。
- 定时列表使用大时间卡片，任务名称、执行规则和下次时间集中展示。
- 定时删除按钮移动到编辑页面，列表只保留启用开关和编辑入口。

## v1.8.0 习惯优化

- 播放队列、进度、播放模式和当前曲目保存到 Room，应用重启后只恢复不自动播放。
- 首页继续播放卡片支持用户确认后继续播放。
- 全局搜索覆盖本地、全部已保存 NAS 和播放列表，当前结果优先展示。
- NAS 文件夹支持名称、修改时间、大小排序，以及音频多选播放和加入队列。
- 本地音乐增加专辑与歌手聚合视图。
- 播放列表多选状态支持批量上移、下移和移除。
- 定时播放遇到 NAS 临时失败时自动重试一次，最终失败通知用户且不回退本地。
- 设置页增加 JSON 备份恢复，密码不会写入备份。

## v1.7.0 Compose 草图接入

- 首页、权限状态、网络音乐、SMB 共享和 NAS 文件夹使用统一毛玻璃列表。
- 最近播放、播放列表和多选播放已经接入三点菜单与批量播放。
- 播放页接入设备栏、封面占位、进度、播放控制、队列拖动和倒计时。
- 定时列表与编辑器接入规范表单、周日选择和文件/文件夹选择。
- 设置页接入动态主题色块、深色模式、缓存和权限卡片。
- 全局底部导航和迷你播放器使用设计组件，主题切换会实时影响主色。

## v1.6.1 设计草图落地

- 按 `PRODUCT_DESIGN_SPEC.md` 和 `design/sketches.html` 调整 Kotlin + Jetpack Compose 主应用。
- 保留现有分层、Hilt、Room、Media3 和功能页面架构。
- 主要页面顶栏改为透明样式，内容与主题渐变背景融合。
- 迷你播放器改为深色悬浮卡片，统一封面占位、歌曲信息和播放控制。
- NAS 文件夹增加毛玻璃概览卡片和播放全部入口。
- 设置页颜色主题改为可视化渐变选择块。
- Flutter 参考工程仅保留为设计实现参考，不替换主应用架构。

## v1.6.0 设计规范落地

- 16 个核心页面草图已生成到 `design/sketches.html`。
- 应用使用透明页面、主题渐变背景和统一毛玻璃卡片。
- 网络文件夹增加文件夹概览和播放全部入口。
- 本地、网络、最近、播放列表和队列歌曲行统一三点菜单。
- 设置页增加可视化颜色主题色块。

## v1.5.2 毛玻璃列表

- 本地音乐、网络文件夹、最近播放、播放列表和播放队列统一使用毛玻璃歌曲行。
- 每首歌曲右侧提供三点菜单。
- 菜单支持立即播放、下一首播放、加入队列，播放列表和队列额外支持排序或移除。

## v1.5.1 毛玻璃样式

- 全部功能页面使用透明 Scaffold 和统一主题渐变背景。
- 网络连接、浏览器、最近播放、定时任务和播放列表使用半透明毛玻璃卡片。
- 底部导航与迷你播放器保持悬浮层级。

## 仪器测试覆盖

- 应用启动、底部导航、定时和设置页面。
- MediaSession 播放服务连接。
- Android Keystore 密码加密、读取和删除。
- Room 数据库连接信息读写。
- 数据库 v1 到 v2 迁移并保留连接与定时。
- 生成 WAV 文件后由 MediaStore 扫描并进入本地音乐库。
- 精确闹钟在真实 Android 设备环境中安排和取消。
- SMB 网络栈不可达错误映射。
- 最近播放写入与排序。
- 播放列表混合歌曲及排序。
- 定时任务多文件顺序持久化。
- 缓存统计和清理。
- 蓝牙路由管理器初始化。
- 模拟器集成测试覆盖 WebDAV 与 Media3 依赖启动。

## v1.5 功能覆盖

- DS Audio 风格播放页。
- 播放页整合当前队列。
- 当前输出设备名称显示。
- 播放列表点击歌曲立即播放。
- 播放列表多选并播放所选。
- 播放所选按照原播放列表顺序。

## v1.4 功能覆盖

- 网易云音乐风格播放页。
- 黑胶唱片旋转。
- 专辑封面模糊背景。
- 上滑下一首、下滑上一首。
- 播放顺序单图标切换。
- 播放队列拖动排序。

## v1.3.1 修复

- 修复进入播放页或连接投屏设备时可能的 Cast 初始化闪退。
- 播放列表“添加网络”按钮移到正文，避免窄屏顶栏不可见。
- 最近播放移动到独立底部导航入口。
- 新增本地 WAV 播放回归测试。

## v1.3 功能覆盖

- 网络音乐改为文件夹式连接入口。
- 测试按钮仅保留在添加和编辑表单。
- 底部导航、首页、播放页和队列增加过渡动画。
- 队列支持拖动排序和移除。
- 周日选择使用自动换行布局。
- 播放设备、深色模式和颜色主题保持可用。

## v1.2 功能覆盖

- 添加网络音乐面板。
- 网络连接详细列表。
- 定时周日布局。
- 顺序、列表循环和单曲循环映射。
- 设置页移除 NAS 和蓝牙区域。
- 自动深色模式和颜色主题界面。
- 局域网媒体代理 Range 请求。

## 修复版主线程回归

- Android 仪器测试在 `Dispatchers.Main` 入口调用 SMB 连接测试，验证网络操作切换到 IO 线程。
- 修复后在 Android 15 模拟器上重新连接真实 NAS 成功。

## 真实 NAS SMB 验收

### 主机 JVM

- NAS：192.168.2.34:445
- 账号认证：通过
- 共享枚举：通过，共发现 11 个可访问共享
- 共享连接：通过，成功连接 music
- 目录读取：通过，music 根目录读取到 27 个条目

### Android 运行时

- Android 模拟器：Android 15，API 35，x86_64
- 账号认证：通过
- 共享枚举：通过
- music 共享连接：通过
- music 根目录读取：通过
- 验证了修复后的 IO 线程执行路径
- 测试使用的密码未写入源码、测试文件、日志或 Git

## 后续现场验收

当前没有实体 Chromecast 或电视测试设备，已完成 Cast SDK、媒体路由、媒体代理和 Range 测试，最终投屏仍需真实设备验收。


真实 WebDAV 和实际音频流播放仍需在 NAS 上继续确认：

1. WebDAV 浏览目录并播放歌曲。
2. SMB 选择音乐文件并实际播放。
3. NAS 断开和密码错误提示。
4. 锁屏定时播放与倒计时停止。
5. 蓝牙输出切换。
