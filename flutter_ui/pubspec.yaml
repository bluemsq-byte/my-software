name: audio_player_ui
description: Flutter UI implementation based on the audio player design specification.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.27.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
